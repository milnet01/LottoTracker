# CLAUDE.md — review-contract loop log

`CLAUDE.md` is loaded in full by every session on every turn, so its review
history is kept **here** rather than in the document: a row appended forever is
a permanent context cost carried by every future session against an audit trail
almost none of them will read. The document itself carries no loop-log section,
and that absence is correct — `check-doc-facts`' `loop-log` check fires only on
a document that already carries such a table.

Genre: **standard** (pinned). Cap: 3.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-08-20 | 2, cold — genre pinned `standard`; packet carried every file and symbol the document names, checked against the tree, plus the measured facts of the roadmap-DB migration | 1 | 2 | 2 | n/a | **Five verified, five fixed; two dismissed.** Trigger: the roadmap DB became the source of truth (LOTTO-0034), which reversed the Working-conventions bullet that had told sessions to hand-edit `ROADMAP.md`. **Both lanes independently found the same two defects**, the strongest signal in the run. The first is the run's most consequential: `verify_watch.py` was called *"the exception"* that needs no dump, while the paragraph eleven lines above says **four** verifiers need one — seven minus four is three, and `local-CI.sh` runs exactly three unconditionally (`verify_page.py`, `verify_watch.py`, `verify_privacy.py` in its weakened mode). A maintainer splitting the lanes would have moved `verify_page.py` behind the dump gate and silently deleted INV-12..21, 23-25 and 27-30 from every CI run. The second was **collateral from the change being gated**: the new text claimed *"Everything else round-trips"* while the render also drops a trailing full stop from a bold `**Layman:**` line and normalises the file's two id dialects — both measured that day, neither disclosed. **The best finding came from one lane alone and is about the check the new text prescribes:** reviewing a render diff *"by census (ids, statuses, word count)"* cannot detect the one loss the same paragraph warns about, because dropping a full stop moves none of the three — verified by running it (3 words before, 3 after). The prescribed check was weaker than the check actually used to catch the defect. The other lane found the one real Q3: the document declared the DB authoritative and never said **where the store lives**, and it is machine-local (`~/.local/share/ants-terminal/roadmap.sqlite`), outside the repo and in no `.gitignore` — so a fresh clone gets the rendered file, an empty store, and a rule forbidding the only edit it can make. Also fixed: the dependency sentence named `dbus-python` only, while `tray.py` imports PySide6 and one `verify_page.py` case starts it. **One dismissed as unverified** — INV-8 and INV-9 were reported as covered by nothing, and `LOTTO-0009` §11 names `tools/verify_pools.py` for both. **One dismissed as immaterial**: the last bullet routes a read to `ROADMAP.md` against the new query-by-id rule, but every write re-renders, so both routes return the same answer at different cost — the second lane raised it and declined to report it for that reason. **One code-side defect surfaced, not fixed** (a docs gate never edits code): `local-CI.sh`'s header says *"Three of the five verifiers"* and calls `verify_privacy.py` "the fourth", while the body gates four (`sources`, `coverage`, `pools`, `payouts`) out of seven — the header predates `verify_payouts.py`. **4b caught one piece of my own collateral**: the dependency fix said the `verify_page.py` case *imports* PySide6, contradicting line 209's *"`tray.py` is the only file that imports PySide6"*; reworded to *starts it in a subprocess*. Both lanes disclosed that the harness had injected a **stale** copy of the document — carrying the pre-migration bullet — and both re-read from disk. |
| 2 | 2026-08-20 | 2, cold — identical brief, packet rebuilt from disk | 1 | 1 | 3 | n/a | **Five verified, five fixed; none dismissed.** **Three of the five landed on text loop 1 WROTE**, which is the measured collateral pattern at its clearest — every one in text a fix *added*, none in what it deleted. **The run's most consequential finding is pre-existing and came from one lane alone:** *"`tickets.py` is the only bank-specific file"* is false. `watch_sms.py:49` is `INCLUDE = ("lotto", "powerball", "vas00")` — `vas00` is the bank's own reference prefix — and that file's own header names *"the same class of defect as LOTTO-0030 (a filter that silently excluded 366 payouts)"*. A conformer handling a bank wording change would have fixed `tickets.py` alone, leaving a parser that is correct and receives nothing: the LOTTO-0030 failure exactly, and invisible for the same reason. **Two Q3s were loop 1's own gap, both about the store being machine-local:** loop 1 said to re-migrate *"on a fresh clone"* and named no other trigger, though the store cannot tell a `git pull` from a hand edit — so a pull followed by one status flip reverts every pulled item, hidden by the very large diff the next paragraph says to expect. And loop 1 never said the re-render must be **committed**: the render is the only copy that crosses machines, an uncommitted one is a lost item, and nothing catches it because a `.md`-only change skips the gate. **The third Q3 was loop 1's own wording:** *"expect both on the first render, and neither again"* covered the id-dialect normalisation (genuinely one-off) and the trailing-full-stop drop (a standing property of any `**Layman:**` line) with one clause, so a conformer writing a new Layman line could not tell whether what they wrote is what would be stored. Now a standing authoring rule: write it with no trailing full stop. **The LOTTO-0007 route was raised by BOTH lanes and had been dismissed in loop 1 as immaterial — that dismissal was wrong.** Loop 1 tested it on the read side and confirmed `roadmap_query` by id does return the lettered list; both lanes came back on the **write** side, which loop 1 never considered: the old text invited *amending* the list in the file, and a hand edit is reverted by the next write. Recorded here rather than by editing loop 1's row. **One code-side defect surfaced for the second consecutive loop, still not fixed** (a docs gate never edits code): `local-CI.sh:20` and `.github/workflows/ci.yml:7` both say *three* verifiers need the dump, while the script gates four — both predate `verify_payouts.py`, and CLAUDE.md points a conformer at that header as authoritative. One open question left unsettled on purpose: which three of the seven bold `**Layman:**` lines keep their full stop, and why. The fix is worded to claim the observation and no mechanism. Both lanes again disclosed a **stale** harness-injected copy of the document and re-read from disk. |
| 3 | 2026-08-20 | 2, cold — identical brief, packet rebuilt from disk | 2 | 2 | 2 | n/a | **Six verified, six fixed; one dismissed. Cap reached (3 for a standard) — the run files its tail and exits.** **This is a VIOLENT cap, and the number says so: four of the six verified findings landed on text THIS RUN wrote** (checked against loops 1 and 2's ledger rows, not from memory). The run was oscillating — each loop repairing the last — and nothing in the evidence suggests a fourth loop would stop. **Both lanes independently found the same defect, and it is the same SENTENCE loop 2 already repaired**: the mandated review check ("every REMOVED line's text still appears somewhere in the new file") fires on the ~24 bullet lines the id-dialect normalisation legitimately rewrites, so a conformer either blocks a correct render or — the real hazard — waves the check through on the one render where it is loudest. Now compared with markup and trailing full stops stripped from both sides, which is what the orchestrator's own verification did on the day and what the document had never said. **The best single finding is a safety rule that had become a laundering route:** loop 2 said to re-migrate whenever the file changes underneath the store, and a local hand edit *is* such a change — so a conformer finding `ROADMAP.md` modified had two opposite duties, and the "safe" one imports into the store precisely the hand edit the bullet above forbids. Scoped to changes arriving from git, with hand edits named as the exception. **Two pre-existing Q1s, both about invariant coverage, and both are traps:** `verify_pools.py` is annotated `INV-7/INV-11` and actually asserts **INV-22, INV-26's runtime half and INV-31** as well, so someone changing `paying_combinations()` finds no verifier named for INV-26 and skips the run that would catch them; and the "remaining invariants" were given as INV-1/2/5 in LOTTO-0001 §5, while LOTTO-0009 §11 gives **INV-8, INV-9 and INV-10** their own §5 commands — **INV-10 has no verifier at all**, so that one-liner is the only thing checking it. **This also corrects loop 1 a second time**: loop 1 dismissed an INV-8/INV-9 finding as unverified because `verify_pools.py` covers them. It does — and the "remaining" list was still incomplete, which is the sharper claim loop 1 never tested. Two loop-2 gaps closed: "every line you write from now on" had no antecedent (pinned to `Layman:` lines and no other), and "Add one with `roadmap_log`" named no mechanism for writing into an existing item's body — `op:"annotate"` does it, confirmed by dry run before the sentence was written. **One dismissed as immaterial, raised in two separate loops**: line 88's "these three items are greenfield" has no antecedent anywhere in the document; it is justification prose that instructs nothing. **One code-side defect surfaced in all three loops and deliberately never fixed** (a docs gate never edits code): `local-CI.sh:20` and `.github/workflows/ci.yml:6-7` both say *three* verifiers need the dump; the script gates four. Both predate `verify_payouts.py`, and CLAUDE.md line 51 sends a conformer to that header as authoritative. Every lane in all three loops disclosed a **stale** harness-injected copy of the document and re-read from disk — four of four dispatches. |
| 4 | 2026-09-06 | 3, cold — genre pinned `standard`; packet carried `local-CI.sh` and `ci.yml` whole, module and spec outlines, and windows on the passages the document leans on | 4 | 2 | 1 | n/a | **Seven verified, seven fixed; three dismissed. Loop 1 of a NEW run.** The 2026-08-20 run ended at a violent cap; that bar lapses with the text it was measured against, and ten commits had touched the document since. Trigger LOTTO-0063, which names three changes — the gated span is wider, and a cold read covers the file regardless. **All three lanes independently found the same two defects, both in the push-gate paragraph LOTTO-0063 had just added.** § Commands said a documentation-only push *"skips the gate automatically"* and *"exits 0"*, while `local-CI.sh` runs `verify_privacy.py` at full strength on exactly that branch and fails if it fails; its own header says *"A documentation-only push is NOT unchecked"*, and `ci.yml` records dropping `paths-ignore` for the same reason. A conformer would have restored `paths-ignore` believing prose was already unchecked. The same paragraph said the classifier reads `upstream..HEAD`; under the hook it reads `$GATE_RANGES`, which `.githooks/pre-push` exports, so a maintainer would have dropped that plumbing as redundant. **Two lanes found the `period_buckets()` bullet resting on a false premise:** *"every case in `tools/verify_page.py` is renderer-only"*, against § Commands' own list of lifecycle, port, managed-run and theme cases in that file. The load-bearing half survives and is what the verifier's own docstring says — no case invokes the builder for an assertion, and the one case that spawns `serve.py` sets `LOTTO_NO_BUILD`. **Two lanes found the diagram calling the archive source *"no payouts"*** while `check.py::amount()` calls `backfill.payouts()` for archive-era draws; the real asymmetry is `issue`, which is what that function branches on, and both sides of the diagram now say so. **Two lanes found the roadmap-DB convention had no route for a pull and a local hand edit arriving together** — migrate and it launders the edit, do not and the next write reverts the pull — and named no discriminator. Two were added, both executed before being written down: `roadmap_query check_sync:true` reports `file_in_sync`, and `git status --porcelain ROADMAP.md` separates an uncommitted edit from one that arrived as a commit. A hand edit already committed is now stated as indistinguishable rather than papered over. **One lane found the two hardcoded-table claims contradicting** — `TIER_PRICES` called *"the one hardcoded table in the project"* where `DRAW_DAYS` is called the second; both are now count-free. **One finding came from resolving a lane's open question, and was collateral of this run's own fix:** § Commands gave *needs no dump* as the CI-lane test, and `verify_sources.py` reads the archive with no dump — the test is *needs neither*. Caught by 4a's re-read of the section the fix landed in. **Three dismissed as true-but-immaterial:** the bad-port fallback attributed to "the tray" where `supervise.py::_port_or_default()` decides it (the sentence contrasts launch paths, not files); `expiry_warned.json`'s "one writer" naming the entry point rather than the writing function; and *"these three items are greenfield"*, which has no antecedent and has now been dismissed in three separate loops. **Collateral fixed in two neighbouring specs:** LOTTO-0036 carried the same renderer-only generalisation, and LOTTO-0034 quoted the hardcoded-table wording this run changed. **Two code-side copies surfaced and deliberately not fixed** (a docs gate never edits code): `expiry.py` and `serve.py` both restate corrected wording. Every lane disclosed an injected copy of the document and reported it matching what it read from disk — the first run here where the injected copy was not stale. |
| 5 | 2026-09-06 | 3, cold — identical brief, packet rebuilt from disk with a fresh line count and a new window on `tools/verify_page.py`'s child-spawn case | 3 | 2 | 1 | n/a | **Six verified, six fixed; none dismissed. Half landed on text loop 4 wrote**, and every one of those in text a fix ADDED. **All three lanes independently found the same defect, and it was mine**: loop 4's repair of the `period_buckets()` bullet asserted that the case spawning `serve.py` *"sets `LOTTO_NO_BUILD` so no build runs"*. The cited file says the opposite in the comment beside that spawn — `LOTTO_NO_BUILD` gates only the OPENING build, `serve.py` hands the real builder to `make_server` regardless, and a `POST /refresh` in that child once started a real build against live data and the operator's API. What keeps it safe is that the case drives `/settings`. LOTTO-0014 § 4.3 states the same thing independently. A conformer adding a child case would have set the variable, posted `/refresh`, and re-created the recorded defect. **This is 4a step 3's failure in its purest form**: the refuting evidence was already in the orchestrator's own packet window and the sentence was written from recall anyway. One lane went further and found *"the one case"* false as well — that file spawns `serve.py` from a reusable helper used by several cases, all of which set the variable and none of which refreshes; the fix now states the condition rather than a count. **A second loop-4 fix was too loose**: the CI lane was given as the verifiers *needing neither* input, where `verify_watch.py` reads the dump when it is there and carries on when it is not. The lane is now defined as `local-CI.sh` defines it — the verifiers that still run honestly on a fresh clone, failing on missing input being what puts a verifier on the local lane. **Two pre-existing Q-findings, both about what the gate actually covers.** *"It runs everything below"* is false: `local-CI.sh` runs no `python3 -c` invariant command, and the document states two paragraphs later that INV-10 has no verifier at all — so a conformer pushing on a green gate believed INV-10 was checked when nothing had checked it. And the privacy section said the verifier *"compares tracked files against the dump itself, not a guessed pattern"*, which undercuts that section's own rule that an INVENTED reference-shaped string is a leak: an invented reference is absent from the dump by definition, so only the pattern half can catch it, and the verifier runs both. **The Q3 is the one a conformer could not have told they breached:** the no-ticket-data rule was attributed to two named functions rather than stated by scope, so a third notification path — a win alert, a parse-failure alert — was governed by nothing, the pasting rule above it reaching only code, docs and commit messages. Now stated for every notification path including one added later, with the re-buy notice as the bounded exception rather than the pattern. **Collateral fixed in `README.md`**, which carried the same not-a-guessed-pattern framing. **One count removed under the user's standing instruction rather than as a finding**: the entries-not-tickets bullet carried figures that had already drifted from `local-CI.sh`'s own, and now states the unit change without them. Every lane read 487 lines; all three reported the injected copy DIFFERING from disk this time, and named the loop-4 edits as the difference — so the injection is stale-by-a-commit rather than reliably fresh. |

## At the cap — the 2026-08-20 run

*This diagnosis belongs to loops 1 to 3. The run opened on 2026-09-06 is
separate and is still in progress at row 4.*

**Violent cap: 4 of loop 3's 6 verified findings landed on text this run wrote.**
Under `review-contract` § At the cap that ends this document's review — a fresh
run would start at loop 1 against a document whose last two loops were each
repairing the one before, and nothing suggests a fourth would differ.

**It is not a size problem.** At 376 lines this file is unremarkable beside its
own siblings (`docs/specs/LOTTO-0001` and `LOTTO-0013` are several times
larger). The oscillation is concentrated almost entirely in **one bullet** —
the roadmap-DB convention, authored in the same session as its own gate and
grown from 9 lines to about 25 across three loops. The gate was therefore
reviewing fresh, unsettled text rather than a settled document, which is
exactly the condition that produces repair-of-repairs.

**Route:** the remaining risk is in the new bullet's *operational* claims — what
a pull does, what an uncommitted render costs, when to re-migrate. Those are
settled by being used, not by a fourth cold read. The next session that
performs a roadmap write under this convention is the better reviewer, and any
correction it finds is an amendment recording what was actually done, which
does not re-arm this gate.
