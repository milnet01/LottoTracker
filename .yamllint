---
# yamllint settings (LOTTO-0007 u).
extends: default
rules:
  # GitHub Actions requires the bare `on:` key; YAML 1.1 reads it as a
  # boolean, which is what this rule flags. Values are still checked.
  truthy:
    check-keys: false
